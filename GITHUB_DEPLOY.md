# Развертывание 404AI на GitHub Pages

## 📋 Подготовка к загрузке на GitHub

### 1. Создайте репозиторий на GitHub

1. Зайдите на [GitHub](https://github.com)
2. Нажмите "New repository"
3. Назовите репозиторий `404AI` (или любое другое имя)
4. Выберите "Public" (для бесплатного GitHub Pages)
5. НЕ ставьте галочку "Add a README file" (у нас уже есть)
6. Нажмите "Create repository"

### 2. Инициализация Git и загрузка

Откройте терминал/PowerShell в папке проекта:

```bash
# Переход в папку проекта
cd "c:\Users\vadim\OneDrive\Рабочий стол\404AI"

# Инициализация Git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: 404AI dual-agent system"

# Добавление удаленного репозитория
git remote add origin https://github.com/ВАШ_НИК/404AI.git

# Загрузка на GitHub
git push -u origin main
```

## 🚀 Настройка GitHub Pages

### 1. Включение GitHub Pages

1. В вашем репозитории на GitHub перейдите в **Settings**
2. В левом меню выберите **Pages**
3. В разделе "Build and deployment" выберите:
   - **Source**: Deploy from a branch
   - **Branch**: main
   - **Folder**: / (root)
4. Нажмите **Save**

### 2. Ожидание развертывания

- GitHub Pages развернет ваш сайт в течение 1-5 минут
- Сайт будет доступен по адресу: `https://ВАШ_НИК.github.io/404AI`

## 📁 Структура файлов для загрузки

```
404AI/
├── public-agent/
│   └── index.html          # Публичный агент (для GitHub Pages)
├── local-agent/
│   └── index.html          # Локальный агент
├── shared/
│   ├── styles.css          # Общие стили
│   ├── config.js           # Конфигурация
│   └── bridge.js           # Мост коммуникации
├── sounds/
│   └── README.md           # Инструкции для звуковых файлов
├── README.md               # Описание проекта
├── DEPLOYMENT.md           # Руководство по развертыванию
├── TEST.md                 # Инструкция по тестированию
└── GITHUB_DEPLOY.md        # Этот файл
```

## 🔧 Что НЕ нужно загружать

- **Node.js файлы** (если есть) - `package.json`, `server.js`
- **Локальные файлы конфигурации**
- **Временные файлы** и кэш

## 🌐 Настройка кастомного домена (опционально)

### 1. Добавление CNAME

Создайте файл `public-agent/CNAME` с вашим доменом:
```
yourdomain.com
```

### 2. Настройка DNS

У вашего доменного регистратора добавьте CNAME запись:
```
www -> ВАШ_НИК.github.io
```

### 3. Включение домена в GitHub

1. В Settings → Pages в разделе "Custom domain"
2. Введите ваш домен
3. Нажмите "Save"

## 📱 Тестирование после загрузки

### 1. Проверка публичного агента

Откройте в браузере: `https://ВАШ_НИК.github.io/404AI/public-agent/`

### 2. Проверка локального агента

1. Скачайте репозиторий: `git clone https://github.com/ВАШ_НИК/404AI.git`
2. Откройте `local-agent/index.html` в браузере

### 3. Тест коммуникации

1. Откройте оба агента в **одном браузере**
2. Отправьте сообщение в публичном агенте
3. Проверьте получение в локальном агенте

## 🔄 Обновление проекта

Для обновления файлов на GitHub:

```bash
# Добавление изменений
git add .

# Коммит с описанием изменений
git commit -m "Update: описание изменений"

# Загрузка обновлений
git push
```

GitHub Pages автоматически обновится через 1-2 минуты.

## ⚡ Быстрая загрузка (скрипт)

Создайте файл `deploy.bat` в Windows:

```batch
@echo off
echo Загрузка 404AI на GitHub...
git add .
git commit -m "Auto update: %date% %time%"
git push
echo Готово! Сайт обновлен.
pause
```

Запускайте двойным кликом для быстрой загрузки.

## 🐛 Частые проблемы

### Проблема: 404 ошибка
- Убедитесь что в Settings → Pages выбрана правильная ветка
- Проверьте что файл `public-agent/index.html` существует

### Проблема: Стили не загружаются
- Проверьте пути в `href="../shared/styles.css"`
- Убедитесь что папка `shared/` загружена

### Проблема: Сообщения не приходят
- Откройте оба агента в одном браузере
- Проверьте консоль (F12) на ошибки
- Убедитесь что localStorage включен

## 📊 Мониторинг

- GitHub Pages показывает статистику в Insights → Traffic
- Для аналитики добавьте Google Analytics в `public-agent/index.html`

## 🔐 Безопасность

- GitHub Pages работает по HTTPS автоматически
- Все данные хранятся в localStorage браузера
- Для продакшена рассмотрите добавление базовой аутентификации

## 🎉 Готово!

После этих шагов ваш 404AI будет:
- ✅ Доступен онлайн по вашему GitHub Pages URL
- ✅ Работать без бэкенда
- ✅ Иметь красивый дизайн
- ✅ Поддерживать коммуникацию между агентами

Пользователи смогут заходить на ваш сайт и общаться с ИИ, а вы будете получать уведомления в локальном агенте!
