# Руководство по развертыванию 404AI

## Быстрый старт

### 1. Развертывание публичного агента (GitHub Pages)

1. Загрузите содержимое папки `public-agent/` в репозиторий GitHub
2. В настройках репозитория включите GitHub Pages
3. Выберите источник `Deploy from a branch` и ветку `main`
4. Ваш сайт будет доступен по адресу: `https://username.github.io/repository-name`

### 2. Запуск локального агента

1. Откройте файл `local-agent/index.html` в браузере
2. Разрешите уведомления в браузере для получения звуковых оповещений

## Синхронизация между агентами

Система использует localStorage для связи между агентами. Для корректной работы:

1. Откройте публичный агент в одной вкладке браузера
2. Откройте локальный агент в другой вкладке того же браузера
3. Агенты автоматически синхронизируются через общий localStorage

## Режимы работы

### Ручной режим (по умолчанию)
- Пользовательские вопросы отправляются локальному агенту
- Вы получаете уведомления и отвечаете вручную
- Ответы появляются у пользователя

### Авто-режим
- Агент отвечает автоматически рандомными фразами
- Не требует вашего вмешательства
- Переключение: Ctrl+Shift+M или кнопка "Переключить режим"

## Настройка домена

### Пользовательский домен для GitHub Pages

1. В настройках репозитория GitHub Pages добавьте свой домен
2. Добавьте DNS запись CNAME:
   ```
   yourdomain.com -> username.github.io
   ```
3. Создайте файл `CNAME` в папке `public-agent/` с вашим доменом

## Продвинутая настройка

### WebSocket сервер (опционально)

Для реального времени можно добавить WebSocket сервер:

```javascript
// В shared/bridge.js можно заменить localStorage на WebSocket
const ws = new WebSocket('ws://localhost:8080');
```

### Бэкенд на Node.js (опционально)

Если нужна персистентность между сессиями:

1. Установите Node.js
2. Создайте простой Express сервер
3. Замените localStorage на API вызовы

## Тестирование

### Тестовое уведомление
Нажмите кнопку "🔔 Тест уведомление" в локальном агенте

### Тестовые сообщения
Локальный агент автоматически генерирует тестовые сообщения каждые 30 секунд

## Звуковые уведомления

Добавьте звуковые файлы в папку `sounds/`:
- `notification.mp3` - новое сообщение
- `send.mp3` - отправка ответа
- `error.mp3` - ошибка

## Безопасность

- Публичный агент не содержит чувствительных данных
- Все данные хранятся в localStorage браузера
- Для продакшена рекомендуется добавить HTTPS и аутентификацию

## Мониторинг

- Статистика сообщений доступна в локальном агенте
- Логи консоли показывают все события коммуникации
- Можно добавить аналитику через Google Analytics

## Частые проблемы

### Сообщения не доходят
- Убедитесь что оба агента открыты в одном браузере
- Проверьте что localStorage не отключен в настройках браузера
- Откройте консоль для диагностики ошибок

### Нет звуковых уведомлений
- Разрешите уведомления в браузере
- Проверьте что звук не выключен в системе
- Добавьте звуковые файлы в папку sounds/

### Авто-режим не работает
- Проверьте что режим переключен в обоих агентах
- Убедитесь что мост коммуникации работает
- Перезагрузите обе вкладки браузера
